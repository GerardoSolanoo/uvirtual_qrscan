import 'package:flutter/material.dart';

class ProfesoresPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Profesores Page"),
    );
  }
}
